using DiscordBot.CustomCommands.Storage;
using FlatSharp;

namespace DiscordBot.CustomCommands
{
    public static class CustomCommandsHandler
    {
        public static void Init()
        {
            //CustomCommandContainer container = new CustomCommandContainer
        }
    }
}